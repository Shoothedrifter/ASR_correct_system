# Production WebSocket backend URL (no trailing slash).
# This value is overridden by the VITE_WS_BASE GitHub Actions secret at build time.
# Update this if you want to test a production build locally.
VITE_WS_BASE=
